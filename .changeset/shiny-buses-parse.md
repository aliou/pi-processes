---
"@aliou/pi-processes": patch
---

Update `@aliou/sh` from ^0.1.0 to ^0.2.2. Adapts `shell-utils` word stringification to the new AST: structured parameter expansions (`exp`/`slice`/`replace`/`index`), typed arithmetic expressions, and the new `BraceExp`/`ExtGlob` word parts.
