# Smoke test

This scenario verifies the full minimal tool loop in one prompt: start, list, stop, and list again.

## Setup

Load the local extension in Pi. No project files or scripts are required.

## Expected result

The agent starts `smoke-test`, lists it, stops it, and lists again using only the `process` tool for process management.
