Start a background process named "overlay-stream" that runs `bash ./tests/e2e/scripts/continuous-output.sh`. Use the process tool. Do not stop it.

After the process starts, I will manually open `/ps:logs` and verify that live output streams in the logs overlay.
