---
"@aliou/pi-processes": minor
---

Bound in-memory process output by both line count and a new configurable byte budget. `/ps`, `/ps:logs`, and the dock now append live output without repeatedly reading full log files, and show a marker when a burst drops lines.
