Start a background process named "replace-fire" that prints "tick" every second with a single log watch for "error" (non-repeating, should trigger an agent turn). Wait for the watch to fire on a matching line, then use the process update action to replace all watches with a new watch for "critical". The replacement should reset the fired state so the new watch can match future output.
