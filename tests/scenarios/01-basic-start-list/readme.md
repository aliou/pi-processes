# Basic start and list

This scenario verifies that the agent can start a long-running background process with the `process` tool and then list tracked processes.

## Setup

Load the local extension in Pi. No project files or scripts are required.

## Expected result

The agent starts one process named `heartbeat` and `process list` shows it as tracked.
